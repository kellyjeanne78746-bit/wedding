{
  "name": "veronica-and-sam-wedding",
  "private": true,
  "version": "1.0.0",
  "description": "Wedding website for Veronica and Samuel — Cloudflare Pages.",
  "scripts": {
    "deploy": "wrangler pages deploy ./deploy/public",
    "dev": "wrangler pages dev ./deploy/public"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
